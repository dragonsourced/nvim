Snip fori for (int i = 0; i < _; ++i) {<CR>}
Snip forj for (int j = 0; j < _; ++j) {<CR>}
Snip main int main(_)<CR>{<CR>return 0;<CR>}
Snip prf  printf(_);

Snip stdio  #include <stdio.h><CR>_
Snip stdlib #include <stdlib.h><CR>_
