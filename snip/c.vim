Snip fori for (int i = 0; i < _; ++i) {<CR>}
Snip forj for (int j = 0; j < _; ++j) {<CR>}
Snip mai  int main(_)<CR>{<CR>return 0;<CR>}
Snip prf  printf(_);

Snip struc struct _ {<CR>};

Snip _io  #include <stdio.h><CR>_
Snip _lib #include <stdlib.h><CR>_
